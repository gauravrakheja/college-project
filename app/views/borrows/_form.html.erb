<%= form_for Borrow.new, html: {class: 'inline'} do |f| %>
	<%= f.hidden_field :user_id, value:current_user.id %>
	<%= f.hidden_field :book_id, value:book.id %>
	<%= f.button :submit, class: 'waves-effect waves-light btn pulse' do %>
		  <i class="material-icons left">add</i>Borrow
	<% end %>
<% end %> 